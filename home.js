(function(){const input=document.getElementById('home-search'),box=document.getElementById('home-results');let people=[];const esc=(v='')=>String(v).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));const label=p=>`${p.nomeCompleto}${p.annoNascita||p.annoMorte?` (${p.annoNascita||'…'}–${p.annoMorte||'…'})`:''}`;function render(){const terms=input.value.trim().toLocaleLowerCase('it').split(/\s+/).filter(Boolean);if(!terms.length){box.hidden=true;return}const found=people.filter(p=>terms.every(t=>`${p.nomeCompleto} ${p.annoNascita} ${p.annoMorte}`.toLocaleLowerCase('it').includes(t))).slice(0,8);box.innerHTML=found.length?found.map(p=>`<a href="scheda.html?id=${encodeURIComponent(p.id)}" class="block p-3 text-sm hover:bg-gray-50"><strong>${esc(label(p))}</strong>${p.rami?.length?`<span class="block text-[10px] text-[#8b5a2b]">${esc(p.rami.map(r=>r.nome).join(', '))}</span>`:''}</a>`).join(''):'<p class="p-3 text-sm text-gray-500">Nessun risultato.</p>';box.hidden=false}input.addEventListener('input',render);window.IanneceAPI.fetchFamilyMembers().then(d=>{people=d.records||[];input.disabled=false}).catch(()=>{input.placeholder='Ricerca temporaneamente non disponibile'})})();
