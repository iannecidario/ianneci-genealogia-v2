(function(){const box=document.getElementById('branches'),esc=(v='')=>String(v).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));window.IanneceAPI.fetchBranches().then(d=>{box.innerHTML=(d.branches||[]).map(b=>`<details class="bg-white rounded-2xl shadow-sm border border-gray-100 overflow-hidden"><summary class="cursor-pointer list-none p-5"><span class="text-[10px] uppercase tracking-wider text-gray-400">${esc(b.periodo||'Memoria familiare')}</span><h3 class="text-2xl font-bold text-[#4a5d4e]">${esc(b.nome)}</h3><span class="text-xs text-gray-500">${b.persone.length} persone</span></summary><div class="px-5 pb-5 border-t border-gray-50 pt-4">${b.iniziatore?`<p class="text-sm mb-3"><strong>Iniziatore:</strong> ${esc(b.iniziatore)}</p>`:''}${b.origine?`<h4 class="font-bold mb-1">Origine del soprannome</h4><p class="text-sm text-gray-600 whitespace-pre-line mb-4">${esc(b.origine)}</p>`:''}${b.note?`<p class="text-sm text-gray-600 mb-4">${esc(b.note)}</p>`:''}<h4 class="font-bold mb-3">Persone del ramo</h4><div class="divide-y divide-gray-50">${b.persone.map(p=>`<a href="scheda.html?id=${encodeURIComponent(p.id)}" class="flex justify-between gap-3 py-3 text-sm"><strong>${esc(p.nomeCompleto)}</strong><span class="text-gray-500">${esc(p.annoNascita||'')}</span></a>`).join('')||'<p class="text-sm text-gray-500">Nessuna persona collegata.</p>'}</div></div></details>`).join('')}).catch(e=>box.innerHTML=`<div class="bg-white p-8 rounded-2xl text-center text-sm text-gray-600">${esc(e.message)}</div>`)})();
