(function () {
  const box = document.getElementById('branches');
  const esc = (value = '') => String(value).replace(/[&<>'"]/g, character => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;' })[character]);
  const personName = person => [person.nome, person.cognome].filter(Boolean).join(' ') || 'Senza nome';
  window.IanneceAPI.fetchBranches().then(data => {
    box.innerHTML = (data.branches || []).map(branch => `<details class="bg-white rounded-2xl shadow-sm border border-gray-100 overflow-hidden"><summary class="cursor-pointer list-none p-5"><span class="text-[10px] uppercase tracking-wider text-gray-400">${esc(branch.periodo || 'Memoria familiare')}</span><h3 class="text-2xl font-bold text-[#4a5d4e]">${esc(branch.nome)}</h3><span class="text-xs text-gray-500">${branch.persone.length} persone</span></summary><div class="px-5 pb-5 border-t border-gray-50 pt-4">${branch.iniziatore ? `<p class="text-sm mb-3"><strong>Iniziatore:</strong> ${esc(branch.iniziatore)}</p>` : ''}${branch.origine ? `<h4 class="font-bold mb-1">Origine del soprannome</h4><p class="text-sm text-gray-600 whitespace-pre-line mb-4">${esc(branch.origine)}</p>` : ''}${branch.note ? `<p class="text-sm text-gray-600 mb-4">${esc(branch.note)}</p>` : ''}<h4 class="font-bold mb-3">Persone del ramo</h4><div class="divide-y divide-gray-50">${branch.persone.map(person => `<a href="scheda.html?id=${encodeURIComponent(person.id)}" class="grid grid-cols-[3.5rem_1fr] gap-3 py-3 text-sm items-baseline"><span class="text-gray-500">${esc(person.annoNascita || '')}</span><strong>${esc(personName(person))}</strong></a>`).join('') || '<p class="text-sm text-gray-500">Nessuna persona collegata.</p>'}</div></div></details>`).join('');
  }).catch(error => { box.innerHTML = `<div class="bg-white p-8 rounded-2xl text-center text-sm text-gray-600">${esc(error.message)}</div>`; });
})();
