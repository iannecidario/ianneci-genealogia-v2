(function(){const list=document.getElementById('people-list'),status=document.getElementById('people-status'),search=document.getElementById('person-search'),filter=document.getElementById('branch-filter'),count=document.getElementById('people-count');let people=[];const esc=(v='')=>String(v).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));const life=p=>p.annoNascita||p.annoMorte?`(${p.annoNascita||'…'}–${p.annoMorte||'…'})`:'';function visible(){const terms=search.value.trim().toLocaleLowerCase('it').split(/\s+/).filter(Boolean),branch=filter.value;return people.filter(p=>terms.every(t=>`${p.nomeCompleto} ${p.annoNascita} ${p.annoMorte}`.toLocaleLowerCase('it').includes(t))&&(!branch||p.ramoIds.includes(branch)))}function render(){const items=visible();count.textContent=`${items.length} ${items.length===1?'testimonianza familiare':'testimonianze familiari'}`;if(!items.length){list.innerHTML='<div class="bg-white p-8 rounded-2xl border border-gray-100 text-center text-sm text-gray-600">Nessuna persona trovata.</div>';return}const groups=new Map();for(const p of items){const l=p.nomeCompleto.trim().charAt(0).toUpperCase()||'?';if(!groups.has(l))groups.set(l,[]);groups.get(l).push(p)}list.innerHTML=[...groups].map(([l,m])=>`<section><h3 class="text-sm font-bold text-gray-400 mb-3 px-2">${esc(l)}</h3><div class="space-y-4">${m.map(p=>`<a href="scheda.html?id=${encodeURIComponent(p.id)}" class="block bg-white p-4 rounded-2xl shadow-sm border border-gray-100 flex items-center gap-4"><div class="w-16 h-16 shrink-0 rounded-full bg-[#d3daea] overflow-hidden flex items-center justify-center">${p.foto?`<img src="${esc(p.foto)}" class="w-full h-full object-cover" alt="">`:'<span class="material-symbols-outlined text-[#4a5d4e] text-3xl">person</span>'}</div><div class="min-w-0"><h4 class="font-bold text-lg truncate">${esc(p.nomeCompleto)} ${esc(life(p))}</h4>${p.rami?.length?`<span class="text-[10px] font-bold text-[#8b5a2b] uppercase">${esc(p.rami.map(r=>r.nome).join(', '))}</span>`:''}</div></a>`).join('')}</div></section>`).join('')}search.addEventListener('input',render);filter.addEventListener('change',render);window.IanneceAPI.fetchFamilyMembers().then(d=>{people=d.records||[];(d.branches||[]).forEach(b=>filter.insertAdjacentHTML('beforeend',`<option value="${esc(b.id)}">${esc(b.nome)}</option>`));status.hidden=true;list.hidden=false;search.disabled=false;filter.disabled=false;render()}).catch(e=>{status.innerHTML=`<span class="material-symbols-outlined text-[#8b5a2b] text-4xl">cloud_off</span><p class="mt-3 text-sm">${esc(e.message)}</p>`})})();
