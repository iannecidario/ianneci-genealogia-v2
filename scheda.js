(function(){const content=document.getElementById('person-content'),title=document.getElementById('page-title'),id=new URLSearchParams(location.search).get('id');const esc=(v='')=>String(v).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));const show=v=>v?esc(v):'—',date=v=>v?new Intl.DateTimeFormat('it-IT').format(new Date(`${v}T00:00:00`)):'—';const rel=(p,label)=>`<a href="scheda.html?id=${encodeURIComponent(p.id)}" class="p-4 flex items-center gap-3"><div class="w-10 h-10 rounded-full bg-[#d3daea] overflow-hidden flex items-center justify-center">${p.foto?`<img src="${esc(p.foto)}" class="w-full h-full object-cover" alt="">`:'<span class="material-symbols-outlined text-[#4a5d4e]">person</span>'}</div><div><p class="font-bold text-sm">${esc(p.nomeCompleto)}</p><p class="text-[10px] text-gray-500">${esc(label)}${p.annoNascita?` · ${esc(p.annoNascita)}`:''}</p></div></a>`;function error(m){content.innerHTML=`<div class="p-4"><div class="bg-white rounded-2xl p-8 text-center"><h2 class="text-xl font-bold">Scheda non disponibile</h2><p class="text-sm text-gray-600 mt-2">${esc(m)}</p></div></div>`}if(!id){error('Identificativo mancante.');return}window.IanneceAPI.fetchPerson(id).then(({person:p})=>{title.textContent=p.nomeCompleto;const parents=[...(p.padre||[]).map(x=>rel(x,'Padre')),...(p.madre||[]).map(x=>rel(x,'Madre'))],spouses=(p.matrimoni||[]).flatMap((m,i)=>m.coniugi.map(x=>rel(x,`Coniuge · Unione ${i+1}`))),children=(p.figli||[]).map(x=>rel(x,'Figlio/a'));content.innerHTML=`<div class="p-4"><section class="bg-[#4a5d4e] rounded-3xl p-8 text-center text-white relative overflow-hidden"><div class="absolute top-4 left-1/2 -translate-x-1/2 bg-white/20 px-3 py-1 rounded-full text-[10px]">ID persona: ${show(p.idPersona)}</div><h2 class="text-3xl font-bold mt-4 mb-2">${esc(p.nomeCompleto)}</h2>${p.rami?.length?`<p class="text-[#e7e0cf] text-xs font-bold uppercase mb-1">${esc(p.rami.map(r=>r.nome).join(', '))}</p>`:''}<p class="text-white/80 text-sm italic mb-8">${show([p.annoNascita,p.annoMorte].filter(Boolean).join(' – '))}</p><button id="photo-button" ${p.foto?'':'disabled'} class="w-48 h-48 mx-auto rounded-2xl border-4 border-white/30 bg-white/10 overflow-hidden flex items-center justify-center shadow-2xl">${p.foto?`<img src="${esc(p.foto)}" class="w-full h-full object-cover" alt="Ritratto di ${esc(p.nomeCompleto)}">`:'<span class="material-symbols-outlined text-7xl text-white/70">person</span>'}</button></section></div><div class="px-4 mb-6"><section class="bg-white rounded-2xl p-6 border border-gray-100 shadow-sm"><h3 class="text-xl font-bold mb-3">Notizie</h3><p class="text-gray-600 text-sm whitespace-pre-line">${show(p.note)}</p></section></div><div class="px-4 mb-6"><section class="bg-white rounded-2xl p-6 border border-gray-100 shadow-sm"><h3 class="text-xl font-bold mb-4">Dati personali</h3><dl class="space-y-4">${[['Data di nascita',date(p.dataNascita)],['Luogo di nascita',show(p.luogoNascita)],['Abitazione',show(p.abitazione)],['Residenza',show(p.residenza)],['Data di morte',date(p.dataMorte)],['Luogo di morte',show(p.luogoMorte)]].map(([k,v])=>`<div class="flex justify-between gap-4 border-b border-gray-50 pb-2"><dt class="text-xs text-gray-500 font-bold uppercase">${k}</dt><dd class="text-sm text-right">${v}</dd></div>`).join('')}</dl></section></div><div class="px-4 mb-6"><h3 class="text-xl font-bold mb-4">Legami familiari</h3><div class="bg-white rounded-2xl overflow-hidden border border-gray-100 shadow-sm divide-y divide-gray-50">${[...parents,...spouses,...children].join('')||'<p class="p-6 text-sm text-gray-600">Nessun legame disponibile.</p>'}</div></div>${p.matrimoni?.length?`<div class="px-4 mb-6"><h3 class="text-xl font-bold mb-4">Matrimoni e unioni</h3><div class="space-y-3">${p.matrimoni.map((m,i)=>`<section class="bg-white rounded-2xl p-5 border border-gray-100 shadow-sm"><h4 class="font-bold">Unione ${i+1}</h4><p class="text-sm text-gray-600 mt-1">${show(m.titolo)}</p><p class="text-xs text-gray-500 mt-2">${show([date(m.data)!=='—'?date(m.data):m.anno,m.luogo].filter(Boolean).join(' · '))}</p>${m.note?`<p class="text-sm text-gray-600 mt-2">${esc(m.note)}</p>`:''}</section>`).join('')}</div></div>`:''}${p.rami?.length?`<div class="px-4 mb-6"><a href="rami.html" class="w-full bg-[#f0f3ff] text-[#4a5d4e] py-4 rounded-2xl font-bold flex justify-center gap-2"><span class="material-symbols-outlined">family_history</span>Vedi ramo familiare</a></div>`:''}<div class="px-4 mb-8"><a href="albero.html?id=${encodeURIComponent(p.id)}" class="w-full bg-[#f0f3ff] text-[#4a5d4e] py-4 rounded-2xl font-bold flex justify-center gap-2"><span class="material-symbols-outlined">account_tree</span>Vedi nell’albero</a></div>`;if(p.foto)document.getElementById('photo-button').addEventListener('click',()=>{const overlay=document.createElement('div');overlay.className='fixed inset-0 z-[100] bg-black/90 p-4 flex items-center justify-center';overlay.innerHTML=`<button aria-label="Chiudi fotografia" class="absolute top-5 right-5 text-white text-4xl">×</button><img src="${esc(p.foto)}" class="max-w-full max-h-full object-contain" alt="Ritratto di ${esc(p.nomeCompleto)}">`;overlay.addEventListener('click',()=>overlay.remove());document.body.appendChild(overlay)})}).catch(e=>error(e.message))})();
